<resources>
    <!-- Base application theme. -->
    <style name="Theme.MyApp" parent="@android:style/Theme.DeviceDefault">
        <!-- Compose MaterialTheme에서 다루지 않는 요소가 있다면 여기서 테마를 커스터마이즈합니다. -->
        <!-- 또는 Wear Compose MaterialTheme만 독점적으로 사용한다면 매우 최소화될 수 있습니다. -->
    </style>
</resources>
